""" 
    expenses app.
"""

##Django
from django.apps import AppConfig

class ExpensesConfig(AppConfig):
    """
    expenses app config
    """
    name = 'rest.expenses'
    verbose_name = 'expenses'